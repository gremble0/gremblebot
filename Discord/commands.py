import random
import discord

def handle_command(client, message):
	#voice_client = discord.VoiceClient(client, message.channel)

	if content == "!ping":
		return "pong!"
	
	if content == "!play":
		return "asd"	
